from graphing.ViewPort import ViewPort

class IFractal:
    """
    A class to represent the different types of fractals. Each different
    """
    def __init__(self, name: str, viewport: ViewPort):
        self.name = name
        self.viewport = viewport
        